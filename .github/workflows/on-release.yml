name: On Release

on:
  release:
    types:
      - published

permissions:
  contents: write

jobs:
  publish:
    uses: ./.github/workflows/publish.yml
    with:
      tag: ${{ github.event.release.tag_name }}
    secrets: inherit
